type EntityNames = "player" | "cow";

type EntityInfo = {

}

type EntitiesRecord = Record<EntityNames, EntityInfo>;

const ENTITIES: EntitiesRecord = {
   player: {

   },
   cow: {

   }
}

export default ENTITIES;