export const nerdVision = {
   isVisible: (() => {}) as () => boolean,
   setIsVisible: (() => {}) as (newIsVisible: boolean) => void,
   terminalIsVisible: (() => {}) as () => boolean,
   setTerminalIsVisible: (() => {}) as (newTerminalIsVisible: boolean) => void,
};